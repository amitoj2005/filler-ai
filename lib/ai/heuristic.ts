// TODO: greedy heuristic AI for bootstrap phase (before neural net is trained)
// - pickMove(state, player): for each legal color, simulate flood fill and
//   count gained cells; return the color that maximizes immediate gain
