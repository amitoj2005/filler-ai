// TODO: board generation
// - Define Color enum (6 values) and Board/Cell types
// - generateBoard(): produce a 7×8 grid where no two adjacent cells share a color
//   (constrained random via rejection sampling)
// - Board is pure data; no I/O
